import { ChevronRight } from 'lucide-react';

function Arrow_right_Icon({size, strokeWidth, color}) {
  return (
    <ChevronRight size={size} strokeWidth={strokeWidth} color={color} />
  );
};

export default Arrow_right_Icon;
