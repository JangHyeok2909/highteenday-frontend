import { ChevronLeft } from 'lucide-react';

function Arrow_Left_Icon({size, strokeWidth, color}) {
  return (
    <ChevronLeft color={color} size={size} strokeWidth={strokeWidth} />
  );
};

export default Arrow_Left_Icon;
